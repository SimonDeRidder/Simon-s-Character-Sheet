name: release
run-name: release assets
on:
  push:
    tags: ["v*.*.*"]
permissions:
  contents: write
jobs:
  build:
    name: Compile and build wasm
    runs-on: [self-hosted, linux, x64]
    container:
      image: rust:1.93.0-alpine
    steps:
      - uses: actions/checkout@v4
      - run: apk add musl-dev tar zip
      - run: rustup target add wasm32-unknown-unknown
      - run: cargo install -f wasm-bindgen-cli --version 0.2.108
      - run: cargo install -f wasm-pack
      - run: wasm-pack build -m no-install --no-typescript -t web -d wasm --out-name wasm --no-pack --release
      - run: rm -r src target Cargo.* *.sh rustfmt.toml
      - run: tar -czvf Simon-s-Character-Sheet-${{github.ref_name}}_SRD.tar.gz *
      - run: zip -r Simon-s-Character-Sheet-${{github.ref_name}}_SRD.zip * -x *.tar.gz
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            *.tar.gz
            *.zip